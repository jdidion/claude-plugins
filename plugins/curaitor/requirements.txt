# Python dependencies for curaitor plugin scripts.
# Install: pip install -r requirements.txt

requests-oauthlib>=1.3.1
pyyaml>=6.0
# certifi is pulled in transitively by requests-oauthlib but pinned here as a
# declared dependency because our scripts import it directly (see scripts/_ssl_util.py)
# to sidestep Python 3.14 strict SSL checks against macOS keychain certs.
certifi>=2025.1.31
pyzotero>=1.5.0
